export default class Ride {

    constructor(origin, destination, availableSeats) {
        this._origin = origin;
        this._destination = destination;
        this._availableSeats = availableSeats;
    }

}